import SwiftUI

struct SettingsView: View {
    var body: some View {
        ScrollView {
            VStack {
                Text("Settings").foregroundColor(Color(hex: "#3B82F6"))
                Divider()
            }
            .padding(16)
        }
    }
}
