import SwiftUI

struct DetailView: View {
    var body: some View {
        ScrollView {
            VStack {
                Text("Detail").foregroundColor(Color(hex: "#3B82F6"))
                Divider()
            }
            .padding(16)
        }
    }
}
