import SwiftUI

struct CardView: View {
    var body: some View {
        VStack {
            Text("title").foregroundColor(Color(hex: "#3B82F6"))
            Divider()
        }
        .padding(16)
    }
}
