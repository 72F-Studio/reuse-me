name: reuse-me
version: 0.1.0
description: Repository health, declaration, usage, and component reuse audit for Hermes Agent, bundled skill, and slash command.
author: Component Intent Audit
provides_hooks:
  - pre_gateway_dispatch
provides_commands:
  - reuse-me
provides_skills:
  - reuse-me
